import React from "react";
import Base from "../core/Base";

const UserDashboard = () => {
     return (
          <Base title="UserDashboard Page" description="User Profile Page.!">
               <h1> This is a UserDashboard Page </h1>
          </Base>
     );
};

export default UserDashboard;
